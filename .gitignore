node_modules/
dist/
dist-electron/
out/
*.tgz
.DS_Store

# The root .gitignore excludes `build/` (Python). Re-include the app's build
# assets dir (electron-builder icon source + generated .icns/.png) but keep the
# transient iconset / master PNG out.
!build/
build/icon.iconset/
build/icon-1024.png
# Bundled sidecar binaries — regenerated by `npm run build:resources`
resources/opencode-server
resources/trie-mcp
resources/trie-cli
resources/bun
resources/migration/
resources/.pyinstaller-work/
